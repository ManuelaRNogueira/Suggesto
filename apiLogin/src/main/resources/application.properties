spring.application.name=backend

# O Render injeta a porta em PORT; local continua em 8080.
server.port=${PORT:8080}

spring.datasource.url=jdbc:mysql://143.106.241.4:3306/cl204225?sslMode=DISABLED&allowPublicKeyRetrieval=true&serverTimezone=America/Sao_Paulo&nullCatalogMeansCurrent=true
spring.datasource.username=cl204225
# A senha do banco vem da variável de ambiente DB_PASSWORD (nunca commitada).
spring.datasource.password=${DB_PASSWORD:}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Credenciais do Cloudinary vêm de variáveis de ambiente (nunca commitadas).
# Sem elas configuradas, o upload de fotos cai para o disco local automaticamente.
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME:}
cloudinary.api-key=${CLOUDINARY_API_KEY:}
cloudinary.api-secret=${CLOUDINARY_API_SECRET:}